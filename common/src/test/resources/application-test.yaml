spring:
  jpa:
    properties:
      hibernate:
        dd-auto: crate
        defalt_schema: elnino
  datasource:
    url: jdbc:h2:mem:elnino;DB_CLOSE_ON_EXIT=FALSE
    driver-class-name: org.h2.Driver
    username: sa
    password:
    initialization-mode: embedded
    platform: h2
    name: elnino
  h2:
    console:
      enabled: true
